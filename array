var arrayExample = [3,4,5,'hello']
function theLastOne(arr) {
   return arr[arr.length - 1];
}
    console.log(theLastOne([3,4,5,'hello']))
	
	function makeLocation(Rexburg, state, zipcode) {
  return '' + city + ', ' + state + ' ' + zipcode;
}
var region = makeLocation('Logan', 'UT', '84321');
console.log(region);
// Outputs: Logan, UT 84321


function theLastOne() {
  var lastElement = /* */;

  return lastElement;
}
